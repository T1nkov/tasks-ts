// Создайте переменную str и присвойте ей строковое значение. Используя цикл for
// выведите каждый символ строки и его индекс

const str1: string = "adasda";

for (let i = 0; i < str1.length; i++) {
  console.log(str[i], i);
}
